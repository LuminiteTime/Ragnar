services:
<<<<<<< 86d0a2e6e035453f176c4d02981d6d5692764bbf
=======
  ollama:
    volumes:
      - ollama:/root/.ollama
    container_name: ollama
    pull_policy: always
    tty: true
    restart: unless-stopped
    image: ollama/ollama:0.2.0

>>>>>>> 5ccda32cb4baf251ab8d0330be0d29594baacb76
  ragnar:
    build:
      context: .
      dockerfile: Dockerfile
    image: luminitetime/ragnar_final:latest
    container_name: ragnar
    volumes:
      - ragnar:/app/backend/data
    ports:
      - ${OPEN_WEBUI_PORT-3000}:8080
    environment:
      - 'WEBUI_SECRET_KEY='
    extra_hosts:
      - host.docker.internal:host-gateway
    restart: unless-stopped

volumes:
  ragnar: {}
